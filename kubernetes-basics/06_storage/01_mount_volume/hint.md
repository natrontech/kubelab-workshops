**hint**: Add volume of type `hostPath` to the pod `logger` and mount it using a `volumeMount`.
You can find the documentation [here](https://kubernetes.io/docs/concepts/storage/volumes/#hostpath-configuration-example).
